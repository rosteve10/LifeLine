<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hospitaladmin">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StaffDetails"></activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".PatientProfile" />
        <activity
            android:name=".AppointmentListDetails"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".StaffDashboard"
            android:theme="@style/AppTheme1" />
        <activity android:name=".HospitalDetailsUpdate" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".staff_registration" />
        <activity
            android:name=".dashboard"
            android:theme="@style/AppTheme1" />
        <activity android:name=".staff_login" />
        <activity android:name=".hospital_registration2" />
        <activity android:name=".hospital_registration1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>